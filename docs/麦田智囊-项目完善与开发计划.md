# "麦田智囊" 项目完善与开发实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 将"麦田智囊"从当前 demo 状态推进到可完整演示、模块集成的生产级系统

**架构：** 四层 Agent 架构（感知→规划→执行→记忆），基于 LangChain LCEL 编排，Chroma + BGE-Large-zh 向量检索，Streamlit 前端 + FastAPI 后端

**技术栈：** Python 3.11, LangChain 0.2.x, DeepSeek-V2 API, ChromaDB, BGE-Large-zh, EasyOCR, FastAPI, Streamlit, Docker

**工程化增强：** CHANGELOG 版本管理、结构化日志（logging 封装）、FastAPI Swagger 接口文档

---

## 现状分析

### 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| Demo 极速备课 | ✅ 可用 | 10秒生成乡土化教案 |
| Demo 智慧传承 | ✅ 可用 | OCR识别+结构化 |
| 主项目 6 个 Agent 框架 | ✅ 代码完整 | 含 BaseAgent + Router |
| RAG 三层知识库框架 | ✅ 代码完整 | Chroma + BGE-Large-zh |
| 记忆系统框架 | ✅ 代码完整 | 对话记忆 + 教师画像 |
| 工具层框架 | ✅ 代码完整 | ASR/OCR/File |
| FastAPI REST API | ✅ 7个端点 | 但接口签名不一致 |
| Streamlit 前端 | ✅ 5/6页面可用 | 主项目前端 |

### 核心问题

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| **模块集成断裂** | 🔴 严重 | RAG、记忆、工具层代码完整但未被 Agent 实际调用 |
| **Agent 接口不一致** | 🔴 严重 | `run()` 签名与基类不匹配（str vs Dict） |
| **Demo 3页面占位** | 🟡 中等 | 课堂伴教、素材推荐、教研纪要均为 disabled |
| **Dockerfile 为空** | 🟡 中等 | 无法容器化部署 |
| **配置管理未使用** | 🟡 中等 | pydantic-settings 定义了但没人用 |
| **伪检索** | 🟡 中等 | 课堂伴教"检索"实际只是 LLM 生成 |

---

## 第一部分：Demo 完善与部署（短期可交付）

### Task 1: 实现 Demo 课堂伴教 Agent

**Files:**
- Create: `demo/agents/classroom_companion.py`
- Modify: `demo/app.py` (render_classroom_companion 函数)
- Test: `tests/test_demo_classroom_companion.py`

- [ ] **Step 1: 创建 ClassroomCompanionAgent 类**

```python
# demo/agents/classroom_companion.py
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
import os

class ClassroomCompanionAgent:
    """课堂实时伴教 Agent - Demo 版本"""

    def __init__(self):
        self.llm = ChatOpenAI(
            model=os.getenv("MODEL_NAME", "deepseek-v2"),
            api_key=os.getenv("OPENAI_API_KEY"),
            base_url=os.getenv("OPENAI_API_BASE"),
            temperature=0.7,
        )

    def generate_quiz(self, subject, grade, topic, knowledge_points,
                      question_count=5, question_types=None):
        """生成练习题"""
        if question_types is None:
            question_types = ["选择题", "填空题"]
        types_str = "、".join(question_types)
        prompt = ChatPromptTemplate.from_template("""
        你是一位经验丰富的{grade}{subject}教师。请根据以下信息生成练习题：

        课题：{topic}
        知识点：{knowledge_points}
        题目数量：{question_count} 道
        题目类型：{types_str}

        要求：
        1. 题目难度适合{grade}学生
        2. 结合乡村生活情境出题（如农田、集市、家庭等场景）
        3. 每道题附标准答案和解析
        4. 格式清晰，编号规范
        """)
        chain = prompt | self.llm | StrOutputParser()
        return chain.invoke({
            "subject": subject, "grade": grade, "topic": topic,
            "knowledge_points": knowledge_points,
            "question_count": question_count, "types_str": types_str,
        })

    def retrieve_classic_questions(self, subject, grade, topic):
        """检索经典题目（Demo 版本使用 LLM 推荐，完整版将接入 RAG 知识库）"""
        prompt = ChatPromptTemplate.from_template("""
        你是一位资深{grade}{subject}教师，拥有丰富的教学经验。

        课题：{topic}

        请推荐 3-5 道经典练习题，要求：
        1. 这些题目是该课题的"必做题"，能帮助学生掌握核心概念
        2. 难度由易到难排列
        3. 结合乡村生活情境
        4. 每道题附答案和教学建议

        注：当前为 Demo 版本，题目由大模型推荐。完整版将接入 RAG 知识库检索真实经典题库。
        """)
        chain = prompt | self.llm | StrOutputParser()
        return chain.invoke({"subject": subject, "grade": grade, "topic": topic})
```

- [ ] **Step 2: 修改 demo/app.py 启用课堂伴教页面**

在 `render_classroom_companion()` 函数中：
- 移除所有 `disabled=True`
- 移除 `st.info("🚧 功能开发中...")`
- 添加功能切换（生成练习题 / 检索经典题）
- 添加题目数量 slider、题目类型 multiselect
- 调用 `ClassroomCompanionAgent` 并展示结果

- [ ] **Step 3: 编写测试**

```python
# tests/test_demo_classroom_companion.py
def test_classroom_companion_generate_quiz():
    agent = ClassroomCompanionAgent()
    result = agent.generate_quiz(
        subject="数学", grade="三年级", topic="分数",
        knowledge_points="分数的认识、比较大小",
        question_count=3, question_types=["选择题"]
    )
    assert isinstance(result, str)
    assert len(result) > 50

def test_classroom_companion_retrieve():
    agent = ClassroomCompanionAgent()
    result = agent.retrieve_classic_questions(
        subject="数学", grade="三年级", topic="分数"
    )
    assert isinstance(result, str)
    assert len(result) > 50
```

- [ ] **Step 4: 运行测试验证**

Run: `pytest tests/test_demo_classroom_companion.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add demo/agents/classroom_companion.py demo/app.py tests/test_demo_classroom_companion.py
git commit -m "feat(demo): 实现课堂伴教 Agent 功能"
```

---

### Task 2: 实现 Demo 素材推荐 Agent

**Files:**
- Create: `demo/agents/material_agent.py`
- Modify: `demo/app.py` (render_material_recommend 函数)
- Test: `tests/test_demo_material_agent.py`

- [ ] **Step 1: 创建 MaterialAgent 类**

```python
# demo/agents/material_agent.py
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
import os

class MaterialAgent:
    """具象化素材推荐 Agent - Demo 版本"""

    def __init__(self):
        self.llm = ChatOpenAI(
            model=os.getenv("MODEL_NAME", "deepseek-v2"),
            api_key=os.getenv("OPENAI_API_KEY"),
            base_url=os.getenv("OPENAI_API_BASE"),
            temperature=0.7,
        )

    def recommend_materials(self, subject, grade, topic,
                           knowledge_points="", rural_context=""):
        """推荐教学素材"""
        prompt = ChatPromptTemplate.from_template("""
        你是一位教育技术专家，擅长为乡村教师推荐教学素材。

        学科：{subject}
        年级：{grade}
        课题：{topic}
        知识点：{knowledge_points}
        乡村情境：{rural_context}

        请按以下分类推荐教学素材，每类 2-3 个：

        ## 📹 科普视频
        推荐适合课堂播放的科普视频，说明视频主题和教学用途。

        ## 🧊 3D 模型 / 动画演示
        推荐可帮助学生理解抽象概念的 3D 模型或动画。

        ## 🖼️ 图片素材
        推荐可用于课件制作的图片素材。

        ## 🎮 互动教具
        推荐适合乡村课堂的互动教学工具或实验方案。

        要求：
        1. 素材需适合{grade}学生认知水平
        2. 优先推荐免费、易获取的资源
        3. 结合乡村实际，推荐可用日常物品替代的实验方案
        4. 每个推荐附简要说明和教学建议

        注：当前为 Demo 版本，推荐由大模型生成。完整版将接入真实素材库检索。
        """)
        chain = prompt | self.llm | StrOutputParser()
        return chain.invoke({
            "subject": subject, "grade": grade, "topic": topic,
            "knowledge_points": knowledge_points,
            "rural_context": rural_context,
        })
```

- [ ] **Step 2: 修改 demo/app.py 启用素材推荐页面**

在 `render_material_recommend()` 函数中：
- 移除所有 `disabled=True` 和占位提示
- 添加学科、年级、课题、知识点、乡村情境输入控件
- 调用 `MaterialAgent.recommend_materials()` 并展示结果

- [ ] **Step 3: 编写测试并运行**

```python
# tests/test_demo_material_agent.py
def test_material_recommend():
    agent = MaterialAgent()
    result = agent.recommend_materials(
        subject="科学", grade="四年级", topic="水的三态变化",
        knowledge_points="固态、液态、气态",
        rural_context="农村冬天结冰现象"
    )
    assert isinstance(result, str)
    assert any(kw in result for kw in ["视频", "模型", "动画", "图片"])
```

Run: `pytest tests/test_demo_material_agent.py -v`

- [ ] **Step 4: Commit**

```bash
git add demo/agents/material_agent.py demo/app.py tests/test_demo_material_agent.py
git commit -m "feat(demo): 实现素材推荐 Agent 功能"
```

---

### Task 3: 实现 Demo 教研纪要 Agent

**Files:**
- Create: `demo/agents/meeting_notes.py`
- Modify: `demo/app.py` (render_meeting_notes 函数)
- Test: `tests/test_demo_meeting_notes.py`

- [ ] **Step 1: 创建 MeetingNotesAgent 类**

```python
# demo/agents/meeting_notes.py
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
import os

class MeetingNotesAgent:
    """教研纪要生成 Agent - Demo 版本"""

    def __init__(self):
        self.llm = ChatOpenAI(
            model=os.getenv("MODEL_NAME", "deepseek-v2"),
            api_key=os.getenv("OPENAI_API_KEY"),
            base_url=os.getenv("OPENAI_API_BASE"),
            temperature=0.3,
        )

    def generate_meeting_notes(self, transcript):
        """从会议记录生成结构化教研报告"""
        prompt = ChatPromptTemplate.from_template("""
        你是一位专业的教研活动记录员。请根据以下会议记录生成结构化的教研报告。

        ## 原始会议记录
        {transcript}

        请按以下格式输出：

        # 教研活动纪要

        ## 一、会议基本信息
        - 会议主题：（从内容中提取）
        - 参与人员：（从内容中提取）
        - 会议时间：（从内容中提取，如未提及则标注"待补充"）

        ## 二、议题与讨论
        （提炼主要议题和讨论要点）

        ## 三、讨论摘要
        （按议题逐条总结讨论内容）

        ## 四、决议事项
        （明确列出达成的共识和决定）

        ## 五、行动计划
        | 序号 | 行动项 | 负责人 | 截止时间 |
        |------|--------|--------|---------|

        ## 六、教研成果
        （总结本次教研的核心成果和收获）

        ## 七、下次教研安排
        （如有提及）
        """)
        chain = prompt | self.llm | StrOutputParser()
        return chain.invoke({"transcript": transcript})
```

- [ ] **Step 2: 修改 demo/app.py 启用教研纪要页面**

在 `render_meeting_notes()` 函数中：
- 移除所有 `disabled=True` 和占位提示
- 添加文本输入区域（st.text_area）
- 保留"上传音频文件（待开发）"选项但标注为未来功能
- 调用 `MeetingNotesAgent.generate_meeting_notes()` 并展示结果

- [ ] **Step 3: 编写测试并运行**

```python
# tests/test_demo_meeting_notes.py
def test_meeting_notes_generation():
    agent = MeetingNotesAgent()
    transcript = """
    今天下午两点，三年级数学组在会议室开展了教研活动。
    参加人员有张老师、李老师、王老师。
    主要讨论了如何提高分数单元的教学效果。
    张老师分享了使用苹果分瓣来讲解分数的经验，效果很好。
    大家决定下次教研由李老师分享几何教学经验。
    """
    result = agent.generate_meeting_notes(transcript)
    assert isinstance(result, str)
    assert any(kw in result for kw in ["决议", "行动计划", "教研成果"])
```

Run: `pytest tests/test_demo_meeting_notes.py -v`

- [ ] **Step 4: Commit**

```bash
git add demo/agents/meeting_notes.py demo/app.py tests/test_demo_meeting_notes.py
git commit -m "feat(demo): 实现教研纪要 Agent 功能"
```

---

### Task 4: Demo Docker 部署配置

**Files:**
- Create: `demo/Dockerfile`
- Create: `demo/docker-compose.yml`
- Create: `demo/start.sh`
- Modify: `demo/README.md`

- [ ] **Step 1: 编写 Demo Dockerfile**

```dockerfile
# demo/Dockerfile
FROM python:3.11-slim

# EasyOCR 系统依赖
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgl1-mesa-glx libglib2.0-0 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# 安装 Python 依赖
COPY ../requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制 demo 代码
COPY demo/ .

# 创建上传目录
RUN mkdir -p /app/assets

# 环境变量默认值
ENV MODEL_NAME=deepseek-v2
ENV OPENAI_API_BASE=https://api.deepseek.com/v1

EXPOSE 8501

CMD ["streamlit", "run", "app.py", \
     "--server.port=8501", \
     "--server.address=0.0.0.0", \
     "--server.headless=true"]
```

- [ ] **Step 2: 编写 demo docker-compose.yml**

```yaml
# demo/docker-compose.yml
version: '3.8'
services:
  maitian-demo:
    build:
      context: ..
      dockerfile: demo/Dockerfile
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./assets:/app/assets
    restart: unless-stopped
```

- [ ] **Step 3: 创建一键启动脚本**

```bash
#!/bin/bash
# demo/start.sh
set -e

if [ ! -f .env ]; then
    cp .env.example .env
    echo "⚠️  已创建 .env 文件，请编辑填入你的 OPENAI_API_KEY"
    echo "   运行: nano demo/.env 或 code demo/.env"
    exit 1
fi

echo "🚀 启动麦田智囊 Demo..."
streamlit run demo/app.py --server.port=8501
```

- [ ] **Step 4: 更新 demo/README.md**

添加以下内容：
- Docker 部署说明（docker-compose up）
- 一键启动说明（bash start.sh）
- 环境变量配置说明

- [ ] **Step 5: 验证 Docker 构建**

Run: `docker build -f demo/Dockerfile -t maitian-demo ..`
Expected: 构建成功

- [ ] **Step 6: Commit**

```bash
git add demo/Dockerfile demo/docker-compose.yml demo/start.sh demo/README.md
git commit -m "feat(demo): 添加 Docker 部署配置和一键启动脚本"
```

---

### Task 5: Demo 全流程冒烟测试

**Files:**
- Create: `tests/test_demo_smoke.py`

- [ ] **Step 1: 编写冒烟测试**

```python
# tests/test_demo_smoke.py
import pytest

def test_demo_imports():
    """验证 demo 所有模块可正常导入"""
    from demo.agents.quick_lesson_prep import QuickLessonPrepAgent
    from demo.agents.wisdom_transfer import WisdomTransferAgent
    from demo.agents.classroom_companion import ClassroomCompanionAgent
    from demo.agents.material_agent import MaterialAgent
    from demo.agents.meeting_notes import MeetingNotesAgent

def test_demo_app_import():
    """验证 demo app.py 可正常导入"""
    import demo.app

def test_all_agents_instantiable():
    """验证所有 Agent 可正常实例化"""
    from demo.agents.quick_lesson_prep import QuickLessonPrepAgent
    from demo.agents.wisdom_transfer import WisdomTransferAgent
    from demo.agents.classroom_companion import ClassroomCompanionAgent
    from demo.agents.material_agent import MaterialAgent
    from demo.agents.meeting_notes import MeetingNotesAgent

    agents = [
        QuickLessonPrepAgent(),
        WisdomTransferAgent(),
        ClassroomCompanionAgent(),
        MaterialAgent(),
        MeetingNotesAgent(),
    ]
    for agent in agents:
        assert agent is not None
```

- [ ] **Step 2: 运行冒烟测试**

Run: `pytest tests/test_demo_smoke.py -v`
Expected: 全部 PASS

- [ ] **Step 3: Commit**

```bash
git add tests/test_demo_smoke.py
git commit -m "test(demo): 添加全流程冒烟测试"
```

---

### Task 5M: 里程碑 — CHANGELOG 初始化 & Demo 版本记录

> 📌 **工程化增强**：每完成一个大阶段新增 CHANGELOG.md 记录版本迭代，体现版本管理意识。

**Files:**
- Create: `CHANGELOG.md`

- [ ] **Step 1: 创建 CHANGELOG.md 并记录 v0.1.0-demo**

采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 格式：

```markdown
# Changelog

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本管理遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-05-03

### 新增 (Added)
- **Demo 极速备课 Agent**：10秒生成乡土化教案，支持学科/年级/课题/乡村情境参数
- **Demo 智慧传承 Agent**：手写教案 OCR 识别 + LLM 结构化处理
- **Demo 课堂伴教 Agent**：练习题生成 + 经典题推荐（LLM 模式）
- **Demo 素材推荐 Agent**：按类型推荐教学素材（视频/3D模型/动画/图片/教具）
- **Demo 教研纪要 Agent**：会议记录文本 → 结构化教研报告
- **Demo Docker 部署**：Dockerfile + docker-compose + 一键启动脚本
- **Demo 冒烟测试**：全流程自动化验证

### 技术细节 (Technical)
- 前端：Streamlit 单页应用，侧边栏导航 6 页面
- 核心编排：LangChain LCEL（ChatPromptTemplate + ChatOpenAI + StrOutputParser）
- 大模型：DeepSeek-V2（OpenAI 兼容接口）
- OCR：EasyOCR（简体中文 + 英文）
- 容器化：Python 3.11-slim 基础镜像
```

- [ ] **Step 2: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: 初始化 CHANGELOG.md，记录 v0.1.0-demo 版本"
```

---

## 第二部分：主项目核心集成（中期目标）

### Task 6: 统一 Agent 接口签名

**Files:**
- Modify: `maitian_agent/agents/quick_lesson_prep.py`
- Modify: `maitian_agent/agents/wisdom_transfer.py`
- Modify: `maitian_agent/api/routes.py`
- Modify: `maitian_agent/cli.py`
- Modify: `maitian_agent/frontend/streamlit_app.py`
- Create: `tests/test_agent_interface.py`

- [ ] **Step 1: 修复 QuickLessonPrepAgent.run() 签名**

将 `run(self, subject, grade, topic, rural_context, **kwargs) -> str` 改为：
```python
def run(self, input_data: Dict[str, Any], **kwargs) -> Dict[str, Any]:
    subject = input_data.get("subject", "")
    grade = input_data.get("grade", "")
    topic = input_data.get("topic", "")
    rural_context = input_data.get("rural_context", "")
    result = self.generate_lesson_plan(subject, grade, topic, rural_context)
    return self._format_output(result)
```
保留 `generate_lesson_plan()` 公开方法供直接调用。

- [ ] **Step 2: 修复 WisdomTransferAgent.run() 签名**

将 `run(self, image_path: str, **kwargs) -> str` 改为：
```python
def run(self, input_data: Dict[str, Any], **kwargs) -> Dict[str, Any]:
    image_path = input_data.get("image_path", "")
    result = self.recognize_handwriting(image_path)
    return self._format_output(result)
```

- [ ] **Step 3: 编写接口一致性测试**

```python
# tests/test_agent_interface.py
import pytest
from maitian_agent.agents.base import BaseAgent

@pytest.mark.parametrize("agent_class,module_path", [
    ("QuickLessonPrepAgent", "maitian_agent.agents.quick_lesson_prep"),
    ("WisdomTransferAgent", "maitian_agent.agents.wisdom_transfer"),
    ("ClassroomCompanionAgent", "maitian_agent.agents.classroom_companion"),
    ("MaterialAgent", "maitian_agent.agents.material_agent"),
    ("MeetingNotesAgent", "maitian_agent.agents.meeting_notes"),
    ("RouterAgent", "maitian_agent.agents.router"),
])
def test_agent_inherits_base(agent_class, module_path):
    mod = __import__(module_path, fromlist=[agent_class])
    cls = getattr(mod, agent_class)
    assert issubclass(cls, BaseAgent)

def test_run_accepts_dict_and_returns_dict():
    from maitian_agent.agents.quick_lesson_prep import QuickLessonPrepAgent
    agent = QuickLessonPrepAgent()
    result = agent.run({"subject": "数学", "grade": "三年级", "topic": "分数", "rural_context": "分苹果"})
    assert isinstance(result, dict)
    assert "success" in result
    assert "result" in result
```

- [ ] **Step 4: 更新 API routes 适配新签名**

修改 `routes.py` 中 `/api/lesson-prep` 和 `/api/wisdom-transfer` 端点，将参数打包为 dict 传入 `agent.run(input_data)`。

- [ ] **Step 5: 更新 CLI 适配新签名**

修改 `cli.py` 中 `lesson_prep` 和 `wisdom_transfer` 命令。

- [ ] **Step 6: 更新 Streamlit 前端适配新签名**

修改 `streamlit_app.py` 中对应页面的 `agent.run()` 调用。

- [ ] **Step 7: 运行全部测试**

Run: `pytest tests/test_agent_interface.py -v`
Expected: 全部 PASS

- [ ] **Step 8: Commit**

```bash
git add maitian_agent/agents/ maitian_agent/api/ maitian_agent/cli.py maitian_agent/frontend/ tests/
git commit -m "refactor: 统一 Agent run() 接口签名，修复与 BaseAgent 不一致问题"
```

---

### Task 7: 集成配置管理

**Files:**
- Modify: `maitian_agent/config/settings.py`
- Modify: `maitian_agent/agents/base.py`
- Modify: `maitian_agent/agents/*.py` (所有 Agent)
- Modify: `maitian_agent/api/routes.py`
- Create: `tests/test_config_integration.py`

- [ ] **Step 1: 重构 BaseAgent 使用 Settings**

在 `BaseAgent.__init__()` 中：
- 添加可选参数 `settings: Optional[Settings] = None`
- 未传入时从 `get_settings()` 获取
- 提取 `_create_default_llm()` 方法，使用 `settings.openai_api_key`、`settings.model_name`、`settings.openai_api_base`

- [ ] **Step 2: 逐个 Agent 接入 Settings**

每个 Agent 的 `__init__()` 中：
- 移除 `os.getenv()` 调用和 `load_dotenv()`
- 改为通过 `self.settings` 获取配置
- 调用 `self._create_default_llm()` 创建 LLM

- [ ] **Step 3: API 入口统一 load_dotenv**

在 `routes.py` 的 `startup_event()` 中调用 `load_dotenv()` 一次。

- [ ] **Step 4: 编写配置集成测试**

```python
# tests/test_config_integration.py
def test_settings_override():
    from maitian_agent.config.settings import Settings
    s = Settings(openai_api_key="test-key", model_name="test-model")
    assert s.openai_api_key == "test-key"
    assert s.model_name == "test-model"

def test_agents_use_settings():
    from maitian_agent.agents.quick_lesson_prep import QuickLessonPrepAgent
    from maitian_agent.config.settings import Settings
    s = Settings(openai_api_key="test-key", model_name="test-model")
    agent = QuickLessonPrepAgent(settings=s)
    assert agent.settings.model_name == "test-model"
```

- [ ] **Step 5: 运行测试**

Run: `pytest tests/test_config_integration.py -v`

- [ ] **Step 6: Commit**

```bash
git add maitian_agent/config/ maitian_agent/agents/ maitian_agent/api/ tests/
git commit -m "refactor: 集成 pydantic-settings 配置管理到所有 Agent"
```

---

### Task 8: 集成工具层

**Files:**
- Modify: `maitian_agent/agents/wisdom_transfer.py`
- Modify: `maitian_agent/agents/meeting_notes.py`
- Modify: `maitian_agent/agents/quick_lesson_prep.py`
- Modify: `maitian_agent/api/routes.py`
- Create: `tests/test_tools_integration.py`

- [ ] **Step 1: WisdomTransferAgent 接入 OCRProcessor**

- 在 `__init__()` 中增加 `ocr_processor` 可选参数
- 默认使用 `OCRProcessor.create(ocr_type=settings.ocr_type)`
- `recognize_handwriting()` 改用 `self.ocr_processor.recognize(image_path)`
- 移除直接的 `from easyocr import Reader`

- [ ] **Step 2: MeetingNotesAgent 接入 WhisperASR**

- 在 `__init__()` 中增加 `asr` 可选参数
- `transcribe_audio()` 改为调用 `self.asr.transcribe(audio_path)`

- [ ] **Step 3: QuickLessonPrepAgent 接入语音备课**

- 在 `__init__()` 中增加 `asr` 可选参数
- `run_with_voice()` 调用 ASR 转写后解析参数再备课

- [ ] **Step 4: API 添加文件上传端点**

- 添加 `POST /api/upload` 端点
- 修改 `POST /api/wisdom-transfer` 支持文件上传
- 添加 `POST /api/meeting-notes/audio` 端点

- [ ] **Step 5: 编写测试并运行**

Run: `pytest tests/test_tools_integration.py -v`

- [ ] **Step 6: Commit**

```bash
git add maitian_agent/agents/ maitian_agent/api/ maitian_agent/tools/ tests/
git commit -m "feat: 集成工具层（OCR/ASR/File）到 Agent 和 API"
```

---

### Task 9: 集成 RAG 知识库

**Files:**
- Modify: `maitian_agent/agents/quick_lesson_prep.py`
- Modify: `maitian_agent/agents/classroom_companion.py`
- Modify: `maitian_agent/agents/wisdom_transfer.py`
- Modify: `maitian_agent/agents/meeting_notes.py`
- Modify: `maitian_agent/api/routes.py`
- Create: `tests/test_rag_integration.py`
- Create: `tests/fixtures/` (测试知识文档)

- [ ] **Step 1: 创建 RAG 集成测试基础设施**

- 创建 `conftest.py` 定义 KnowledgeBase fixture（使用临时目录）
- 创建测试 fixture 预填充测试文档
- 验证 `add_document()` 和 `search()` 基本功能

- [ ] **Step 2: QuickLessonPrepAgent 集成 RAG**

- `__init__()` 增加 `knowledge_base` 可选参数
- prompt 增加 `{reference_materials}` 占位符
- `run()` 中先检索知识库，将参考材料注入 prompt
- `knowledge_base` 为 None 时向后兼容

- [ ] **Step 3: ClassroomCompanionAgent 集成真实 RAG**

- `retrieve_classic_questions()` 改为真正调用 `knowledge_base.search()`
- 将检索结果传入 prompt
- 无 knowledge_base 时退回 LLM 生成模式

- [ ] **Step 4: WisdomTransferAgent 集成知识库入库**

- `save_to_knowledge_base()` 实现真正的入库逻辑
- `run()` 中结构化完成后自动入库
- 返回结果中添加入库状态

- [ ] **Step 5: MeetingNotesAgent 集成知识库入库**

- 生成报告后将教研成果部分入库

- [ ] **Step 6: API 暴露知识库管理端点**

- `POST /api/knowledge/add`
- `GET /api/knowledge/search`
- `GET /api/knowledge/stats`

- [ ] **Step 7: 编写测试并运行**

Run: `pytest tests/test_rag_integration.py -v`

- [ ] **Step 8: Commit**

```bash
git add maitian_agent/agents/ maitian_agent/api/ maitian_agent/rag/ tests/
git commit -m "feat: 集成 RAG 知识库到 Agent 执行流程"
```

---

### Task 10: 集成记忆系统

**Files:**
- Modify: `maitian_agent/agents/base.py`
- Modify: `maitian_agent/agents/quick_lesson_prep.py`
- Modify: `maitian_agent/agents/wisdom_transfer.py`
- Modify: `maitian_agent/api/routes.py`
- Create: `tests/test_memory_integration.py`

- [ ] **Step 1: ConversationMemory 集成到 BaseAgent**

- `BaseAgent.__init__()` 增加 `conversation_memory` 可选参数
- 添加 `_save_to_memory()` 方法
- `_format_output()` 后自动保存对话

- [ ] **Step 2: TeacherProfile 集成到备课 Agent**

- prompt 中注入教师风格信息
- 每次成功生成后递增 `lesson_plans_count`

- [ ] **Step 3: TeacherProfile 集成到传承 Agent（风格学习）**

- 结构化后用 LLM 提取教学风格标签
- 保存到 TeacherProfile

- [ ] **Step 4: API 暴露记忆和画像管理端点**

- `POST /api/teachers/profile`
- `GET /api/teachers/profile/{teacher_id}`
- `GET /api/teachers/profiles`

- [ ] **Step 5: 编写测试并运行**

Run: `pytest tests/test_memory_integration.py -v`

- [ ] **Step 6: Commit**

```bash
git add maitian_agent/agents/ maitian_agent/memory/ maitian_agent/api/ tests/
git commit -m "feat: 集成记忆系统（对话记忆+教师画像）到 Agent"
```

---

### Task 11: Agent 工厂与依赖注入

**Files:**
- Create: `maitian_agent/agents/factory.py`
- Modify: `maitian_agent/agents/__init__.py`
- Modify: `maitian_agent/api/routes.py`
- Modify: `maitian_agent/frontend/streamlit_app.py`

- [ ] **Step 1: 创建 AgentFactory**

```python
# maitian_agent/agents/factory.py
class AgentFactory:
    """统一管理所有 Agent 的创建和依赖注入"""

    def __init__(self, settings=None, knowledge_base=None,
                 conversation_memory=None, teacher_profile_manager=None):
        self.settings = settings or get_settings()
        self.knowledge_base = knowledge_base
        self.conversation_memory = conversation_memory
        self.teacher_profile_manager = teacher_profile_manager

    def create_agent(self, agent_type: str) -> BaseAgent:
        """创建指定类型的 Agent，自动注入依赖"""
        deps = {
            "settings": self.settings,
            "knowledge_base": self.knowledge_base,
            "conversation_memory": self.conversation_memory,
        }
        # 根据 agent_type 实例化对应 Agent
        ...

    def create_all_agents(self) -> Dict[str, BaseAgent]:
        """批量创建所有 Agent"""
        ...
```

- [ ] **Step 2: 重构 API routes 使用 AgentFactory**

- `startup_event()` 中使用 `AgentFactory.create_all_agents()`
- 统一注入所有依赖

- [ ] **Step 3: 重构 Streamlit 前端使用 AgentFactory**

- 修改 `init_agents()` 使用工厂

- [ ] **Step 4: Commit**

```bash
git add maitian_agent/agents/ maitian_agent/api/ maitian_agent/frontend/
git commit -m "refactor: 创建 AgentFactory 统一依赖注入"
```

---

### Task 12: 完善 Docker 部署

**Files:**
- Modify: `docker/Dockerfile`
- Modify: `docker/docker-compose.yml`
- Create: `maitian_agent/.env.example`

- [ ] **Step 1: 编写主项目 Dockerfile**

```dockerfile
FROM python:3.11-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    libgl1-mesa-glx libglib2.0-0 ffmpeg \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY maitian_agent/ ./maitian_agent/

RUN mkdir -p /app/data/chroma /app/data/uploads /app/data/memory /app/data/profiles

ENV MODEL_NAME=deepseek-v2
ENV OPENAI_API_BASE=https://api.deepseek.com/v1
```

- [ ] **Step 2: 更新 docker-compose.yml**

拆分为 `api` 和 `frontend` 两个服务，共享数据卷，添加健康检查。

- [ ] **Step 3: 创建 .env.example**

包含所有 Settings 字段的环境变量示例和注释。

- [ ] **Step 4: 验证 Docker 构建**

Run: `docker build -f docker/Dockerfile -t maitian-agent .`

- [ ] **Step 5: Commit**

```bash
git add docker/ maitian_agent/.env.example
git commit -m "feat: 完善主项目 Docker 部署配置"
```

---

### Task 12M: 里程碑 — CHANGELOG v0.2.0 & 结构化日志

> 📌 **工程化增强**：阶段完成时更新 CHANGELOG；引入结构化日志封装，方便线上排查。

**Files:**
- Modify: `CHANGELOG.md`
- Create: `maitian_agent/utils/__init__.py`
- Create: `maitian_agent/utils/logger.py`
- Modify: `maitian_agent/agents/base.py`（接入 logger）
- Modify: `maitian_agent/api/routes.py`（接入 logger）
- Create: `tests/test_logger.py`

- [ ] **Step 1: 创建结构化日志封装**

```python
# maitian_agent/utils/logger.py
import logging
import sys
import json
from datetime import datetime
from pathlib import Path


class JSONFormatter(logging.Formatter):
    """结构化 JSON 日志格式化器"""

    def format(self, record):
        log_entry = {
            "timestamp": datetime.utcnow().isoformat() + "Z",
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
            "module": record.module,
            "function": record.funcName,
            "line": record.lineno,
        }
        # 附加自定义字段
        if hasattr(record, "agent_name"):
            log_entry["agent_name"] = record.agent_name
        if hasattr(record, "request_id"):
            log_entry["request_id"] = record.request_id
        if hasattr(record, "duration_ms"):
            log_entry["duration_ms"] = record.duration_ms
        if record.exc_info and record.exc_info[0] is not None:
            log_entry["exception"] = self.formatException(record.exc_info)
        return json.dumps(log_entry, ensure_ascii=False)


def setup_logger(
    name: str = "maitian",
    level: str = "INFO",
    log_file: str | None = None,
    json_format: bool = True,
) -> logging.Logger:
    """
    创建并配置 logger。

    Args:
        name: logger 名称
        level: 日志级别 (DEBUG/INFO/WARNING/ERROR)
        log_file: 日志文件路径，None 则仅输出到 stdout
        json_format: 是否使用 JSON 格式（生产环境推荐 True）

    Returns:
        配置好的 Logger 实例
    """
    logger = logging.getLogger(name)
    logger.setLevel(getattr(logging, level.upper(), logging.INFO))
    logger.handlers.clear()

    formatter = JSONFormatter() if json_format else logging.Formatter(
        "%(asctime)s [%(levelname)s] %(name)s - %(message)s"
    )

    # stdout handler
    stdout_handler = logging.StreamHandler(sys.stdout)
    stdout_handler.setFormatter(formatter)
    logger.addHandler(stdout_handler)

    # file handler（可选）
    if log_file:
        Path(log_file).parent.mkdir(parents=True, exist_ok=True)
        file_handler = logging.FileHandler(log_file, encoding="utf-8")
        file_handler.setFormatter(formatter)
        logger.addHandler(file_handler)

    return logger


# 模块级默认 logger
logger = setup_logger()
```

- [ ] **Step 2: 在 BaseAgent 中接入 logger**

在 `BaseAgent.__init__()` 和 `run()` 方法中添加日志记录：
```python
from maitian_agent.utils.logger import logger

class BaseAgent:
    def run(self, input_data, **kwargs):
        logger.info(
            f"Agent {self.__class__.__name__} 开始执行",
            extra={"agent_name": self.__class__.__name__}
        )
        start = time.time()
        try:
            result = self._execute(input_data, **kwargs)
            duration = (time.time() - start) * 1000
            logger.info(
                f"Agent {self.__class__.__name__} 执行完成",
                extra={"agent_name": self.__class__.__name__, "duration_ms": round(duration, 2)}
            )
            return result
        except Exception as e:
            logger.error(
                f"Agent {self.__class__.__name__} 执行失败: {e}",
                extra={"agent_name": self.__class__.__name__},
                exc_info=True
            )
            raise
```

- [ ] **Step 3: 在 API routes 中接入 logger（含请求 ID）**

在 FastAPI 中间件中为每个请求生成 `request_id`，并在日志中记录：
```python
import uuid
from maitian_agent.utils.logger import logger

@app.middleware("http")
async def log_requests(request, call_next):
    request_id = str(uuid.uuid4())[:8]
    logger.info(f"请求开始 {request.method} {request.url.path}",
                extra={"request_id": request_id})
    response = await call_next(request)
    logger.info(f"请求完成 status={response.status_code}",
                extra={"request_id": request_id})
    return response
```

- [ ] **Step 4: 编写日志测试**

```python
# tests/test_logger.py
def test_json_formatter_output():
    from maitian_agent.utils.logger import JSONFormatter, setup_logger
    import logging, io, json
    fmt = JSONFormatter()
    record = logging.LogRecord("test", logging.INFO, "", 1, "hello", (), None)
    result = json.loads(fmt.format(record))
    assert result["level"] == "INFO"
    assert result["message"] == "hello"
    assert "timestamp" in result

def test_logger_with_custom_fields():
    from maitian_agent.utils.logger import setup_logger
    log = setup_logger("test_custom", level="DEBUG")
    log.info("test", extra={"agent_name": "TestAgent", "duration_ms": 123.4})
```

- [ ] **Step 5: 更新 CHANGELOG.md 记录 v0.2.0**

在 CHANGELOG.md 顶部新增：
```markdown
## [0.2.0] - 2026-05-XX

### 新增 (Added)
- **Agent 接口统一**：所有 Agent run() 签名与 BaseAgent 一致
- **配置管理集成**：pydantic-settings 接入所有模块
- **工具层集成**：OCR/ASR/File 工具类接入 Agent 执行流程
- **RAG 知识库集成**：Agent 可检索知识库并自动入库
- **记忆系统集成**：对话记忆 + 教师画像接入 Agent
- **AgentFactory**：统一依赖注入工厂
- **结构化日志**：JSON 格式日志，支持 request_id 和 duration_ms
- **Docker 部署**：主项目完整 Dockerfile + docker-compose

### 变更 (Changed)
- LangChain 升级至 v1.2（create_agent + @tool + init_chat_model）
- 所有 Agent 从 os.getenv() 迁移到 pydantic-settings

### 修复 (Fixed)
- Agent run() 返回类型不一致（str → Dict）
- ClassroomCompanionAgent 伪检索问题（LLM生成 → 真实RAG检索）
```

- [ ] **Step 6: 运行测试**

Run: `pytest tests/test_logger.py -v`

- [ ] **Step 7: Commit**

```bash
git add maitian_agent/utils/ CHANGELOG.md maitian_agent/agents/base.py maitian_agent/api/routes.py tests/
git commit -m "feat: 添加结构化日志封装，更新 CHANGELOG v0.2.0"
```

---

## 第三部分：功能完善与优化（长期目标）

### Task 13: Whisper ASR 完整集成

**Files:**
- Modify: `maitian_agent/tools/asr.py`
- Modify: `maitian_agent/agents/meeting_notes.py`
- Modify: `maitian_agent/frontend/streamlit_app.py`

- [ ] **Step 1: 完善 WhisperASR 错误处理和重试**
- 添加音频格式验证、文件大小限制、API 重试机制、超长音频分段转写

- [ ] **Step 2: MeetingNotesAgent 音频输入完整流程**
- Streamlit 页面启用音频上传，完整流程：上传→转写→生成报告

- [ ] **Step 3: QuickLessonPrepAgent 语音备课完整流程**
- Streamlit 极速备课页面添加语音输入选项

- [ ] **Step 4: Commit**

```bash
git commit -m "feat: 完成 Whisper ASR 语音输入完整集成"
```

---

### Task 14: BM25 混合检索

**Files:**
- Modify: `maitian_agent/rag/knowledge_base.py`
- Modify: `requirements.txt`

- [ ] **Step 1: 实现 BM25 关键词检索**
- 使用 `rank_bm25` + `jieba` 分词
- `KnowledgeBase` 维护 BM25 索引，添加文档时同步更新

- [ ] **Step 2: 实现混合检索融合**
- 使用 Reciprocal Rank Fusion (RRF) 合并向量和 BM25 结果
- 支持自定义权重

- [ ] **Step 3: Agent 切换到混合检索**

- [ ] **Step 4: Commit**

```bash
git commit -m "feat: 实现 BM25 + 向量混合检索策略"
```

---

### Task 15: 教师风格向量匹配

**Files:**
- Modify: `maitian_agent/memory/teacher_profile.py`
- Modify: `maitian_agent/agents/quick_lesson_prep.py`

- [ ] **Step 1: 实现 TeacherStyleVectorStore**
- 使用 ChromaVectorStore 存储教师风格向量
- `find_similar_teachers()` 查询相似风格教师

- [ ] **Step 2: 备课 Agent 融入风格匹配**
- 备课前查找相似风格教师，注入参考教案片段

- [ ] **Step 3: Commit**

```bash
git commit -m "feat: 实现教师风格向量匹配与个性化备课"
```

---

### Task 16: 文件上传 API 完善

**Files:**
- Modify: `maitian_agent/api/routes.py`
- Modify: `maitian_agent/tools/file.py`

- [ ] **Step 1: 统一文件上传服务**
- 文件类型验证、大小限制、返回文件 URL

- [ ] **Step 2: 批量文件上传**
- `POST /api/upload/batch` 支持多文件

- [ ] **Step 3: Commit**

```bash
git commit -m "feat: 完善文件上传 API（类型校验+批量上传）"
```

---

### Task 17: 流式响应

**Files:**
- Modify: `maitian_agent/agents/base.py`
- Modify: `maitian_agent/agents/quick_lesson_prep.py`
- Modify: `maitian_agent/api/routes.py`
- Modify: `maitian_agent/frontend/streamlit_app.py`

- [ ] **Step 1: Agent 层流式输出支持**
- `BaseAgent` 添加 `run_stream()` 方法
- 各 Agent 实现 chain `stream()` 迭代

- [ ] **Step 2: API 层 SSE 流式端点**
- `POST /api/lesson-prep/stream` 返回 SSE 格式

- [ ] **Step 3: Streamlit 前端流式显示**
- 使用 `st.write_stream()` 逐步渲染

- [ ] **Step 4: Commit**

```bash
git commit -m "feat: 实现流式响应（Agent→API→前端）"
```

---

### Task 18: 教案导出

**Files:**
- Create: `maitian_agent/tools/export.py`
- Modify: `maitian_agent/api/routes.py`
- Modify: `maitian_agent/frontend/streamlit_app.py`

- [ ] **Step 1: 创建 LessonPlanExporter**
- 支持 Markdown 和 Word (.docx) 格式导出
- 使用 `python-docx` 生成结构化 Word 文档

- [ ] **Step 2: API 添加导出端点**
- `GET /api/lesson-prep/export/{format}`

- [ ] **Step 3: Streamlit 添加导出按钮**

- [ ] **Step 4: Commit**

```bash
git commit -m "feat: 支持教案导出为 Markdown 和 Word 格式"
```

---

### Task 18M: 里程碑 — CHANGELOG v0.3.0 & Swagger 接口文档完善

> 📌 **工程化增强**：利用 FastAPI 自带 Swagger 维护接口文档，提升项目完整度。

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `maitian_agent/api/routes.py`（完善 Swagger 文档元数据）
- Modify: `maitian_agent/agents/base.py`（接入 logger，如尚未完成）

- [ ] **Step 1: 完善 FastAPI 应用元数据**

```python
# maitian_agent/api/routes.py
app = FastAPI(
    title="麦田智囊 API",
    description="""
    ## 麦田智囊 — 乡村教育 Agent 系统 API

    基于大模型多智能体技术的乡村教育普惠解决方案，为乡村教师提供 AI 教研搭档。

    ### 核心功能
    - 🎯 **极速备课**：10秒生成乡土化教案
    - 📜 **智慧传承**：手写教案数字化传承
    - 📝 **课堂伴教**：实时出题与经典题检索
    - 🎨 **素材推荐**：具象化教学素材推荐
    - 📊 **教研纪要**：会议记录结构化报告

    ### 认证
    当前版本无需认证（Demo 阶段）。生产版本将支持 API Key 认证。
    """,
    version="0.3.0",
    docs_url="/docs",       # Swagger UI
    redoc_url="/redoc",     # ReDoc
    contact={
        "name": "麦田智囊团队",
        "url": "https://github.com/Komorebi-GZM/wheatfield_braintrust",
    },
    license_info={"name": "MIT"},
)
```

- [ ] **Step 2: 为所有端点添加详细 docstring 和 response_model**

每个端点补充：
- `summary`：简短描述（Swagger 摘要栏）
- `description`：详细说明（Swagger 展开详情）
- `response_model`：Pydantic 响应模型（自动生成响应 Schema）
- `tags`：按功能分组（备课、传承、伴教、素材、纪要、知识库、教师管理）
- `responses`：错误码说明（400/404/500）

示例：
```python
@router.post(
    "/api/lesson-prep",
    summary="极速备课",
    description="根据学科、年级、课题和乡村情境，10秒内生成包含教学目标、重难点、教学过程等完整内容的乡土化教案。",
    response_model=LessonPrepResponse,
    tags=["备课"],
    responses={
        200: {"description": "教案生成成功"},
        400: {"description": "参数缺失或格式错误"},
        500: {"description": "大模型调用失败"},
    },
)
async def lesson_prep(request: LessonPrepRequest):
    ...
```

- [ ] **Step 3: 创建统一的 Pydantic 响应模型**

```python
# maitian_agent/api/schemas.py
from pydantic import BaseModel, Field
from typing import Optional

class AgentResponse(BaseModel):
    """统一 Agent 响应格式"""
    success: bool = Field(description="是否执行成功")
    result: Optional[str] = Field(None, description="Agent 执行结果")
    agent: str = Field(description="Agent 名称")
    error: Optional[str] = Field(None, description="错误信息")
    metadata: Optional[dict] = Field(default_factory=dict, description="附加元数据")

class LessonPrepRequest(BaseModel):
    subject: str = Field(..., description="学科", examples=["数学"])
    grade: str = Field(..., description="年级", examples=["三年级"])
    topic: str = Field(..., description="课题", examples=["分数的认识"])
    rural_context: str = Field("", description="乡村情境", examples=["分苹果"])

class LessonPrepResponse(AgentResponse):
    pass

# ... 其他 Request/Response 模型
```

- [ ] **Step 4: 更新 CHANGELOG.md 记录 v0.3.0**

```markdown
## [0.3.0] - 2026-05-XX

### 新增 (Added)
- **Whisper ASR 语音输入**：教研纪要和极速备课支持语音输入
- **BM25 混合检索**：向量检索 + BM25 关键词检索，RRF 融合
- **教师风格向量匹配**：基于风格相似度推荐教案
- **文件上传 API**：统一上传端点 + 批量上传
- **流式响应**：SSE 端点 + Streamlit 逐步渲染
- **教案导出**：支持 Markdown 和 Word 格式
- **Swagger 接口文档**：完整的 OpenAPI 文档，含请求/响应 Schema
- **结构化日志**：JSON 格式日志，request_id 追踪

### 变更 (Changed)
- LangChain 升级至 v1.2
- API 端点统一使用 Pydantic response_model
```

- [ ] **Step 5: 验证 Swagger 文档**

Run: `uvicorn maitian_agent.api.routes:app --reload`
访问 `http://localhost:8000/docs` 确认 Swagger UI 正常展示所有端点。

- [ ] **Step 6: Commit**

```bash
git add maitian_agent/api/ CHANGELOG.md
git commit -m "docs: 完善 Swagger 接口文档，更新 CHANGELOG v0.3.0"
```

---

## 执行依赖关系

```
第一部分（可并行）
  Task 1 ─┐
  Task 2 ─┼── Task 5（冒烟测试）→ Task 5M（CHANGELOG v0.1.0）
  Task 3 ─┘
  Task 4（独立）

第二部分（严格顺序）
  Task 6（接口统一）→ Task 7（配置集成）→ Task 11（工厂）
                                              ↓
  Task 8（工具层）→ Task 9（RAG）→ Task 10（记忆）→ Task 12（Docker）→ Task 12M（CHANGELOG v0.2.0 + 日志）

第三部分（大部分可并行）
  Task 13 ─┐
  Task 14 ─┤
  Task 15 ─┼── 可根据优先级选择执行 → Task 18M（CHANGELOG v0.3.0 + Swagger）
  Task 16 ─┤
  Task 17 ─┤
  Task 18 ─┘

前置任务（建议在第二部分开始前完成）
  Task 0（LangChain 1.2 升级）→ 影响所有 Agent 代码
```

**推荐执行路径：**
1. Task 0（LangChain 升级）→ Task 1-5 + 5M（Demo完善+CHANGELOG）
2. Task 6-7（基础重构）→ Task 11（工厂）→ Task 8-10（核心集成）→ Task 12 + 12M（部署+日志+CHANGELOG）
3. Task 13-18（功能增强）→ Task 18M（Swagger+CHANGELOG）

---

## 前置任务：LangChain 1.2 升级

> ⚠️ **重要**：当前项目基于 LangChain 0.2.x，需升级到 v1.2。LangChain 1.x 有重大 API 变更，建议在第二部分开始前完成此升级，避免后续重复修改。

### Task 0: LangChain 0.2 → 1.2 升级

**背景**：LangChain 2026 已进入 v1.x 时代，核心变化：
- `create_agent()` 替代旧的 `AgentExecutor`
- `init_chat_model()` 统一模型初始化接口
- `@tool` 装饰器替代旧工具定义方式
- `ChatPromptTemplate` / `StrOutputParser` 保持兼容，但推荐使用字典格式消息
- `langchain-core` 独立包已合并回 `langchain` 主包

**Files:**
- Modify: `requirements.txt`
- Modify: `demo/agents/*.py`（所有 demo Agent）
- Modify: `maitian_agent/agents/*.py`（所有主项目 Agent）
- Modify: `maitian_agent/rag/*.py`
- Modify: `maitian_agent/tools/*.py`
- Create: `tests/test_langchain_migration.py`

- [ ] **Step 1: 更新 requirements.txt**

```
# 核心框架（v1.2）
langchain>=1.2.0,<2.0.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.2.0

# 移除旧版单独包
# langchain-core>=0.2.0  ← 已合并到 langchain
```

- [ ] **Step 2: 统一模型初始化为 init_chat_model()**

所有 Agent 中的 `ChatOpenAI(...)` 替换为：
```python
from langchain.chat_models import init_chat_model

# 旧写法（0.2.x）
# llm = ChatOpenAI(model=os.getenv("MODEL_NAME"), api_key=..., base_url=...)

# 新写法（1.2）
llm = init_chat_model(
    "deepseek-v2",           # 或从 settings 获取
    model_provider="openai",  # 使用 OpenAI 兼容接口
    api_key=settings.openai_api_key,
    base_url=settings.openai_api_base,
    temperature=0.7,
)
```

- [ ] **Step 3: 更新 LCEL 链写法**

LCEL 管道 `prompt | llm | parser` 在 1.2 中仍然支持，但推荐使用字典格式消息：
```python
# 旧写法（仍兼容，但推荐迁移）
prompt = ChatPromptTemplate.from_template("...")
chain = prompt | llm | StrOutputParser()

# 新写法（1.2 推荐，更灵活）
messages = [
    {"role": "system", "content": "你是一位{grade}{subject}教师"},
    {"role": "user", "content": "请为课题「{topic}」生成教案"},
]
response = llm.invoke(messages)
```

- [ ] **Step 4: 更新工具定义为 @tool 装饰器**

```python
# 旧写法
# class BaseTool(BaseTool): ...

# 新写法（1.2）
from langchain.tools import tool

@tool
def search_knowledge_base(query: str) -> str:
    """检索知识库中的相关内容"""
    return knowledge_base.search(query)
```

- [ ] **Step 5: 更新 RAG 相关导入**

```python
# 旧写法
# from langchain_chroma import Chroma
# from langchain_community.embeddings import HuggingFaceBgeEmbeddings

# 新写法（1.2）
from langchain_chroma import Chroma
from langchain_openai import OpenAIEmbeddings  # 推荐使用 OpenAI Embeddings
# 或继续使用 HuggingFace（需确认兼容性）
```

- [ ] **Step 6: 编写迁移验证测试**

```python
# tests/test_langchain_migration.py
def test_langchain_version():
    import langchain
    assert langchain.__version__.startswith("1."), f"Expected 1.x, got {langchain.__version__}"

def test_init_chat_model():
    from langchain.chat_models import init_chat_model
    # 使用 mock 验证接口可用
    model = init_chat_model("gpt-3.5-turbo", model_provider="openai",
                            api_key="test-key", base_url="https://api.example.com/v1")
    assert model is not None

def test_tool_decorator():
    from langchain.tools import tool
    @tool
    def dummy_tool(x: str) -> str:
        """测试工具"""
        return x
    assert dummy_tool.name == "dummy_tool"
    assert dummy_tool.description == "测试工具"
```

- [ ] **Step 7: 全量回归测试**

Run: `pytest tests/ -v`
确保所有现有测试在 LangChain 1.2 下通过。

- [ ] **Step 8: Commit**

```bash
git add requirements.txt demo/agents/ maitian_agent/ tests/
git commit -m "chore: 升级 LangChain 0.2 → 1.2，迁移至新 API"
```

---

## 测试策略：Mock API 调用指南

> 📌 **原则**：所有涉及外部 API（大模型、OCR、ASR）的测试必须使用 mock，确保测试可离线运行、快速稳定。

### Mock 策略总览

| 外部依赖 | Mock 方式 | 适用场景 |
|---------|----------|---------|
| **大模型 (ChatOpenAI)** | `unittest.mock.patch` 替换 `llm.invoke()` | Agent 单元测试 |
| **OCR (EasyOCR)** | `unittest.mock.patch` 替换 `Reader.readtext()` | 智慧传承测试 |
| **ASR (Whisper)** | `unittest.mock.patch` 替换 `client.audio.transcriptions.create()` | 语音转写测试 |
| **向量数据库 (Chroma)** | 使用临时目录 + 真实 Chroma（轻量） | RAG 集成测试 |
| **文件系统** | `tmp_path` pytest fixture | 文件上传测试 |
| **FastAPI 端点** | `TestClient` + mock Agent | API 测试 |

### 完整 Mock 示例

```python
# tests/conftest.py — 共享 fixtures
import pytest
from unittest.mock import MagicMock, patch

@pytest.fixture
def mock_llm():
    """Mock 大模型调用，返回预设文本"""
    with patch("maitian_agent.agents.quick_lesson_prep.ChatOpenAI") as MockChatOpenAI:
        mock_instance = MagicMock()
        mock_instance.invoke.return_value = "这是一份测试教案内容..."
        MockChatOpenAI.return_value = mock_instance
        yield mock_instance

@pytest.fixture
def mock_ocr():
    """Mock OCR 识别，返回预设文本"""
    with patch("maitian_agent.tools.ocr.EasyOCR") as MockEasyOCR:
        mock_reader = MagicMock()
        mock_reader.readtext.return_value = [
            ([0, 0, 100, 100], "三年级数学教案", 0.95),
            ([0, 100, 100, 200], "课题：分数的认识", 0.92),
        ]
        MockEasyOCR.return_value = mock_reader
        yield mock_reader

@pytest.fixture
def mock_asr():
    """Mock Whisper ASR，返回预设转写文本"""
    with patch("maitian_agent.tools.asr.OpenAI") as MockOpenAI:
        mock_client = MagicMock()
        mock_client.audio.transcriptions.create.return_value = "这是一段测试会议记录..."
        MockOpenAI.return_value = mock_client
        yield mock_client


# tests/test_agents_with_mock.py — 完整 Agent 测试示例
class TestQuickLessonPrepAgent:
    """极速备课 Agent 完整测试（含 mock）"""

    def test_run_returns_dict_format(self, mock_llm):
        from maitian_agent.agents.quick_lesson_prep import QuickLessonPrepAgent
        agent = QuickLessonPrepAgent()
        result = agent.run({
            "subject": "数学", "grade": "三年级",
            "topic": "分数", "rural_context": "分苹果"
        })
        assert isinstance(result, dict)
        assert result["success"] is True
        assert "教案" in result["result"]
        assert result["agent"] == "QuickLessonPrepAgent"

    def test_run_handles_llm_error(self):
        from maitian_agent.agents.quick_lesson_prep import QuickLessonPrepAgent
        with patch("maitian_agent.agents.quick_lesson_prep.ChatOpenAI") as MockLLM:
            MockLLM.return_value.invoke.side_effect = Exception("API 超时")
            agent = QuickLessonPrepAgent()
            result = agent.run({
                "subject": "数学", "grade": "三年级",
                "topic": "分数", "rural_context": "分苹果"
            })
            assert result["success"] is False
            assert "error" in result

    def test_run_with_knowledge_base(self, mock_llm):
        """验证 RAG 集成后，参考材料被注入 prompt"""
        from maitian_agent.agents.quick_lesson_prep import QuickLessonPrepAgent
        from maitian_agent.rag.knowledge_base import KnowledgeBase
        # 使用临时目录创建真实 Chroma（轻量，无需 mock）
        kb = MagicMock()
        kb.search.return_value = [{"content": "分数教学应使用生活化场景", "score": 0.95}]
        agent = QuickLessonPrepAgent(knowledge_base=kb)
        result = agent.run({
            "subject": "数学", "grade": "三年级",
            "topic": "分数", "rural_context": "分苹果"
        })
        # 验证 knowledge_base.search 被调用
        kb.search.assert_called_once()
        assert result["success"] is True


class TestWisdomTransferAgent:
    """智慧传承 Agent 完整测试（含 mock）"""

    def test_run_with_mock_ocr(self, mock_ocr):
        from maitian_agent.agents.wisdom_transfer import WisdomTransferAgent
        agent = WisdomTransferAgent()
        result = agent.run({"image_path": "/fake/path/image.jpg"})
        assert isinstance(result, dict)
        assert result["success"] is True
        mock_ocr.readtext.assert_called_once()

    def test_save_to_knowledge_base(self, mock_ocr):
        """验证结构化后自动入库"""
        from maitian_agent.agents.wisdom_transfer import WisdomTransferAgent
        kb = MagicMock()
        kb.add_document.return_value = True
        agent = WisdomTransferAgent(knowledge_base=kb)
        result = agent.run({"image_path": "/fake/path/image.jpg"})
        # 验证入库被调用
        kb.add_document.assert_called_once()


# tests/test_api_with_mock.py — API 测试示例
class TestAPIEndpoints:
    """FastAPI 端点测试（mock Agent 层）"""

    def test_lesson_prep_endpoint(self):
        from fastapi.testclient import TestClient
        from maitian_agent.api.routes import app

        with patch("maitian_agent.api.routes.lesson_prep_agent") as mock_agent:
            mock_agent.run.return_value = {
                "success": True,
                "result": "测试教案",
                "agent": "QuickLessonPrepAgent",
                "metadata": {},
            }
            client = TestClient(app)
            response = client.post("/api/lesson-prep", json={
                "subject": "数学", "grade": "三年级",
                "topic": "分数", "rural_context": "分苹果"
            })
            assert response.status_code == 200
            data = response.json()
            assert data["success"] is True

    def test_wisdom_transfer_upload(self):
        from fastapi.testclient import TestClient
        from maitian_agent.api.routes import app

        with patch("maitian_agent.api.routes.wisdom_transfer_agent") as mock_agent, \
             patch("maitian_agent.api.routes.file_processor") as mock_file:
            mock_file.save_upload.return_value = "/tmp/test_image.jpg"
            mock_agent.run.return_value = {
                "success": True,
                "result": "结构化教案",
                "agent": "WisdomTransferAgent",
                "metadata": {},
            }
            client = TestClient(app)
            # 模拟文件上传
            response = client.post(
                "/api/wisdom-transfer",
                files={"file": ("test.jpg", b"fake_image_data", "image/jpeg")}
            )
            assert response.status_code == 200
```

### Mock 最佳实践

1. **mock 外部依赖，不 mock 内部逻辑**：Agent 内部的 prompt 构建和参数处理应真实执行，只 mock LLM 调用
2. **RAG 使用真实 Chroma**：Chroma 是嵌入式数据库，使用 `tmp_path` 创建临时实例，无需 mock
3. **每个测试独立**：使用 pytest fixture 确保测试间无状态污染
4. **验证交互而非实现**：`assert mock_llm.invoke.called` 而非检查内部方法名
5. **错误路径也要测**：mock `side_effect=Exception` 验证错误处理逻辑

---

## 关键风险与缓解

| 风险 | 缓解措施 |
|------|---------|
| BGE-Large-zh 模型下载慢 | 提供镜像源配置；备选 OpenAI Embeddings |
| EasyOCR Docker 内存不足 | 增加 `--memory=4g`；备选 PaddleOCR |
| DeepSeek API 不稳定 | 支持多 API Base 切换；添加重试降级 |
| **LangChain 1.2 迁移兼容** | **先完成 Task 0 升级，再开始功能开发；编写迁移验证测试** |
| ChromaDB 并发写入 | 使用文件锁；写入重试 |
| langchain-chroma 与 1.2 兼容 | 锁定版本；备选 FAISS 或 langchain-community 向量库 |

---

## 假设与决策

1. **Python 版本**：统一使用 Python 3.11（避免 3.14 兼容性问题）
2. **LangChain 版本**：升级至 v1.2（Task 0），使用 `init_chat_model()` + `@tool` + `create_agent()` 新 API
3. **大模型 API**：继续使用 DeepSeek-V2（OpenAI 兼容接口），通过 `init_chat_model` 统一管理
4. **向量数据库**：使用 Chroma（轻量级），生产环境可升级 Milvus
5. **OCR 方案**：使用 EasyOCR（已验证可用），PaddleOCR 作为备选
6. **Demo 定位**：Demo 保持独立简洁，不引入 RAG/记忆等复杂依赖
7. **主项目定位**：主项目承担完整功能，通过 AgentFactory 管理依赖注入
8. **版本管理**：每完成一个大阶段更新 CHANGELOG.md（v0.1.0-demo → v0.2.0 → v0.3.0）
9. **日志方案**：JSON 结构化日志，生产环境启用文件输出，开发环境使用可读格式
10. **接口文档**：利用 FastAPI 自带 Swagger UI（/docs）+ ReDoc（/redoc），零额外依赖
11. **测试策略**：外部 API 调用一律 mock，RAG 使用真实 Chroma + 临时目录
